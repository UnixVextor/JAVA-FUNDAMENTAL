package OOP;

public abstract class Abstraction {
    /*
        abstract = abstract class cannot be instantiated, but they can have a subclass ->> like virtual
                   abstract method are declared without an implementation
    */
    abstract void go();
}
